/**
 * STUB — TDD red phase (packet 0014). Type shape only; Coder implements the real
 * Card-below, non-overlapping layout wrapping AdSlot. Intentionally incomplete so
 * src/__tests__/packet-0014.test.ts fails on assertions, not on module resolution.
 */
export interface HomeBannerProps {
  adGroupId: string;
}

export function HomeBanner(_props: HomeBannerProps) {
  return null;
}
